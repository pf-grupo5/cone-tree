# Cone tree

## Dependencias

* `bash-completion`
* `cmake`
* `libfmt-dev`
* `libgml-dev`

## Build
``` bash
git clone https://github.com/pf-grupo5/cone-tree
cd cone-tree

mkdir -p build
cd build

cmake ..
make
```
