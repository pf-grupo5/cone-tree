# Cone tree

## Dependencias

* `bash-completion`
* `cmake`
* `libfmt-dev`
* `libglm-dev`

## Build
``` bash
git clone https://github.com/pf-grupo5/cone-tree
cd cone-tree

mkdir -p build
cd build

cmake ..
make
```
